name: demo_vosk_application
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_tts: ^4.0.2
  http: ^0.13.6
  permission_handler: ^10.4.5
  record: ^5.1.0
  vosk_flutter: ^0.3.48
  web_socket_channel: ^3.0.0
  workmanager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

assets:
  - .env